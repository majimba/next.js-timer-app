/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Keep default development indicators
  devIndicators: {
    buildActivity: true,
  },
}

module.exports = nextConfig
